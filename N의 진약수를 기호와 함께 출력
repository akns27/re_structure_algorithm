#include <stdio.h>
int main() {
	int N, i, sum=0;
	scanf("%d", &N);
	printf("1");
	for(i=2; i < N; i++)
	{
		if(N%i==0)
		{
			printf(" + %d", i);
			sum += i;
		}
	}
	printf(" = %d", sum+1);
	return 0;
}
